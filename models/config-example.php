<?php
	// IMPORTANTE: este archivo debe copiarse y renombrarse como config.php.
	// Después, debes rellenar el valor de las siguientes variables.

	class Config {
		public static $dbHost = "";   // Escribe el nombre el host de la base de datos (p. ej: localhost)
		public static $dbUser = "";   // Escribe el nombre de usuario de la base de datos (p. ej: root)
		public static $dbPass = "";   // Escribe la password de ese usuario
		public static $dbName = "";   // Escribe el nombre de la base de datos que vas a usar
		public static $baseUrl = "";  // Escribe la URL base de la aplicación (p. ej: http://localhost/rosenpi)
	}
